-- ONLY TESTED ON SQLITE

-- MAKE URLS
CREATE TABLE "urls" (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    "hash" TEXT NOT NULL,
    "url" TEXT NOT NULL
)

-- MAKE USERS
CREATE TABLE "users" (
    "id" INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    "user" TEXT NOT NULL,
    "password" TEXT NOT NULL,
    "role" TEXT NOT NULL
)
